const initialState = {
	_id: '5cb0ecf9f2ffe53269260943',
	name: 'Ryan Brott',
	pushToken: ''
};

const user = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default user;